<Window x:Class="XyrusWorx.SchemaBrowser.Windows.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:views="clr-namespace:XyrusWorx.SchemaBrowser.Windows.Views"
        mc:Ignorable="d" Title="Schema Browser" Height="450" Width="800"
        Icon="pack://application:,,,/Resources/Images/app.ico">

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="40" />
			<RowDefinition />
		</Grid.RowDefinitions>

		<Border Background="{StaticResource ThemeColorBrush}">
			<StackPanel Orientation="Horizontal">
				<!-- Toolbar -->
			</StackPanel>
		</Border>
		<Border Background="#eee" Grid.Row="1">
			<!-- Client -->
		</Border>

		<views:SplashView Grid.Row="0" Grid.RowSpan="2">
			<views:SplashView.Style>
				<Style>
					<Style.Triggers>
						<DataTrigger Binding="{Binding Path=IsLoading, Mode=OneWay}" Value="False">
							<DataTrigger.EnterActions>
								<BeginStoryboard>
									<Storyboard>
										<DoubleAnimation From="1" To="0" Storyboard.TargetProperty="Opacity" Duration="0:0:0.25" />
									</Storyboard>
								</BeginStoryboard>
							</DataTrigger.EnterActions>
						</DataTrigger>
					</Style.Triggers>
				</Style>
			</views:SplashView.Style>
		</views:SplashView>
	</Grid>
</Window>
